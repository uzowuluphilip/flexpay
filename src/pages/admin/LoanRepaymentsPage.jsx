import { useEffect, useState } from 'react'
import { useNavigate } from 'react-router-dom'
import { adminApi } from '../../lib/api/admin'

export default function LoanRepaymentsPage() {
  const navigate = useNavigate()
  const [status, setStatus] = useState('pending')
  const [repayments, setRepayments] = useState([])
  const [selected, setSelected] = useState(null)
  const [reason, setReason] = useState('')
  const [preview, setPreview] = useState(null)
  const [error, setError] = useState('')
  const [busy, setBusy] = useState(false)
  const load = () => adminApi.listLoanRepayments(status).then((data) => setRepayments(data.repayments || [])).catch((err) => setError(err.message))
  useEffect(() => { load() }, [status])
  const approve = async (id) => { if (!window.confirm('Approve this repayment?')) return; setBusy(true); try { await adminApi.approveLoanRepayment(id); await load() } catch (err) { setError(err.message) } finally { setBusy(false) } }
  const reject = async () => { if (!selected || !reason.trim()) return setError('A rejection reason is required.'); setBusy(true); try { await adminApi.rejectLoanRepayment(selected.id, reason); setSelected(null); setReason(''); await load() } catch (err) { setError(err.message) } finally { setBusy(false) } }
  const showReceipt = async (id) => { try { setPreview(await adminApi.fetchLoanRepaymentReceipt(id)) } catch (err) { setError(err.message) } }
  return <div className="min-h-screen bg-gray-900 text-white"><header className="sticky top-0 z-20 border-b border-gray-700 bg-gray-800"><div className="mx-auto flex max-w-7xl items-center gap-4 px-4 py-4"><button onClick={() => navigate('/admin')} className="text-gray-400 hover:text-white">← Back</button><h1 className="text-2xl font-bold">Loan Repayments</h1></div></header><main className="mx-auto max-w-7xl px-4 py-8">{error ? <p className="mb-4 rounded border border-red-700 bg-red-900 p-3 text-red-200">{error}</p> : null}<div className="mb-6 flex gap-2">{['pending', 'approved', 'rejected'].map((item) => <button key={item} onClick={() => setStatus(item)} className={`rounded px-4 py-2 capitalize ${status === item ? 'bg-blue-600' : 'border border-gray-700 bg-gray-800 text-gray-400'}`}>{item}</button>)}</div><div className="overflow-hidden rounded-lg border border-gray-700 bg-gray-800"><div className="overflow-x-auto"><table className="w-full text-sm"><thead className="bg-gray-700"><tr><th className="px-4 py-3 text-left">User</th><th className="px-4 py-3 text-left">Repayment</th><th className="px-4 py-3 text-left">Loan balance</th><th className="px-4 py-3 text-left">Receipt</th><th className="px-4 py-3 text-center">Actions</th></tr></thead><tbody>{repayments.map((item) => <tr key={item.id} className="border-t border-gray-700"><td className="px-4 py-3"><p className="font-medium">{item.full_name}</p><p className="text-xs text-gray-400">{item.email}</p></td><td className="px-4 py-3 font-semibold">₦{(item.amount_kobo / 100).toLocaleString()}</td><td className="px-4 py-3"><p>Repaid: ₦{(item.amount_repaid_kobo / 100).toLocaleString()}</p><p className="text-gray-400">Total: ₦{(item.total_repayable_kobo / 100).toLocaleString()}</p></td><td className="px-4 py-3"><button onClick={() => showReceipt(item.id)} className="text-blue-300 underline">View receipt</button></td><td className="px-4 py-3 text-center">{item.status === 'pending' ? <div className="flex justify-center gap-2"><button disabled={busy} onClick={() => approve(item.id)} className="rounded bg-green-600 px-3 py-1 text-xs">Approve</button><button disabled={busy} onClick={() => setSelected(item)} className="rounded bg-red-600 px-3 py-1 text-xs">Reject</button></div> : <span className="capitalize text-gray-400">{item.status}</span>}</td></tr>)}</tbody></table></div>{repayments.length === 0 ? <p className="p-8 text-center text-gray-400">No loan repayments found</p> : null}</div>{selected ? <div className="fixed inset-0 z-30 flex items-center justify-center bg-black/75 p-4"><div className="w-full max-w-md rounded-lg bg-gray-800 p-6"><h2 className="text-lg font-semibold">Reject loan repayment</h2><textarea value={reason} onChange={(event) => setReason(event.target.value)} rows="4" placeholder="Reason for rejection" className="mt-4 w-full rounded border border-gray-600 bg-gray-700 p-3 text-white" /><div className="mt-4 flex gap-3"><button onClick={() => { setSelected(null); setReason('') }} className="flex-1 rounded bg-gray-700 px-4 py-2">Cancel</button><button disabled={busy} onClick={reject} className="flex-1 rounded bg-red-600 px-4 py-2">Reject</button></div></div></div> : null}{preview ? <div className="fixed inset-0 z-30 flex items-center justify-center bg-black/80 p-4" onClick={() => { URL.revokeObjectURL(preview.url); setPreview(null) }}><div className="max-h-[90vh] max-w-4xl rounded-lg bg-white p-2" onClick={(event) => event.stopPropagation()}>{preview.type === 'application/pdf' ? <iframe src={preview.url} title="Loan repayment receipt" className="h-[82vh] w-[min(80vw,60rem)]" /> : <img src={preview.url} alt="Loan repayment receipt" className="max-h-[82vh] max-w-full object-contain" />}<button onClick={() => { URL.revokeObjectURL(preview.url); setPreview(null) }} className="mt-2 w-full rounded bg-gray-800 px-4 py-2 text-white">Close</button></div></div> : null}</main></div>
}
